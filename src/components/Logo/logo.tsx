import styles from "./logo.module.css"
import logo from "../../assets/logo.png"

const Logo = () => {
    return (
        <img height={50} src={logo} alt="Website Logo"/>
    )
}

export default Logo;